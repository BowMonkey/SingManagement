#include <stdio.h>
int main(void)
{
	char ch = 9;
	printf("%d", ch);
	getchar();
	/*int num[10];
	int i = 10;
	printf("num大小：%d\n", sizeof(num)/sizeof(int));
	printf("赋值之前的初始值\n");
	for (size_t i = 0; i < sizeof(num) / sizeof(int); i++)
	{
		printf("num[%d]%d\n",i, num[i]);
	}
	printf("为num循环赋值\n");
	for (size_t i = 0; i < sizeof(num) / sizeof(int); i++)
	{
		scanf("%d", &num[i]);
	}
	for (size_t i = 0; i < sizeof(num) / sizeof(int); i++)
	{
		printf("num循环赋值后的值\n%d\n", num[i]);
	}*/
	return 0;
}